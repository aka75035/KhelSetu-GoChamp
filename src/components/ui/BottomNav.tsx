import React from "react";

export default function BottomNav() {
  return (
    <nav className="fixed bottom-0 left-0 w-full bg-white border-t shadow p-3 flex justify-center z-50">
      <span className="text-gray-500">Admin Navigation</span>
    </nav>
  );
}